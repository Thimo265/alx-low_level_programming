#include "main.h"
/**
 * _isdigit - checks for digits in a range from 0 to 9
 * @c: integer to be checked
 * Return: 1 if c is digit and th opposite is true
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
